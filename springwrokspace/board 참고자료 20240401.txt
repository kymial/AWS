### 3. spring boot was 의 port 4100으로 지정
main/resouces/application.properties

최상단에
server.port = 4100(원하는 포트번호) 
추가


### 4. spring boot - mysql company 데이터베이스 연결
main/resouces/application.properties

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/board?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=root


### 5. base package 아래에 controller, service, repository, dto, entity, exception handler, config 패키지 추가 (service에는 implementations 패키지 추가)

src/main/java/com/example/board 아래에 폴더 controller, service, entity, dto, repository, exceptionHandler, config 생성

src/main/java/com/example/board/service에 implementations 폴더 생성


### 6. spring boot cors 정책 지정 (모든 패턴에 모든 http method와 모든 출처에 대하여 허용)

src/main/java/com/example/board/config에서 CorsConfig.java 파일 생성

그리고 내용에

@Configuration

public class CorsConfig implements WebMvcConfigurer{

    @Override
    public void addCorsMappings (CorsRegistry registry) {
        registry
        .addMapping("/**")
            .allowedMethods("*")
                .allowedOrigins("*");
    }
}

추가


### 7. Entity 및 Repository 작성

Entity)


src/main/java/com/example/board/Entity에 UserEntity.java 생성

그리고 

package com.example.board.entity;

import jakarta.persistence.Entity;
import jakarta.persistence.Id;
import jakarta.persistence.Table;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
@AllArgsConstructor
@Entity(name = "user")
@Table(name = "user")

public class UserEntity {
    @Id
    private String email;
    private String password;
    private String nickname;
    private String telNumber;
    private String address;
    private String addressDetail;
    private String profileImageUrl;
    
}

추가



Repository)


src/main/java/com/example/board/repository에서 UserRepository.java 파일 생성

package com.example.board.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import com.example.board.entity.UserEntity;

@Repository

public interface UserRepository extends JpaRepository<UserEntity, String>{
    
}

추가